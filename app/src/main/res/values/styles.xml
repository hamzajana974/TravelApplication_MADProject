<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ImgSplashlogo" parent="@android:style/Widget" >
        <item name="android:layout_height">200dp</item>
        <item name="android:layout_width">200dp</item>
        <item name="android:src">@drawable/travellogo</item>

    </style>
    <style name="txtheading" parent="@android:style/Widget.TextView" >
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:text">@string/txtSplashAppnam</item>
        <item name="android:textSize">@dimen/heading</item>
        <item name="android:textColor">@color/green</item>
        <item name="android:paddingTop">15dp</item>
        <item name="android:textAlignment">center</item>
     <item name="android:textStyle">italic</item>

    </style>
<style name="txtfooter" parent="@android:style/Widget.TextView">
    <item name="android:layout_width">match_parent</item>
    <item name="android:layout_height">wrap_content</item>
    <item name="android:text">@string/txtSplashFooter</item>
    <item name="android:textAlignment">center</item>
    <item name="android:textColor">@color/white</item>
    <item name="android:textSize">@dimen/splashFooter</item>

</style>


</resources>